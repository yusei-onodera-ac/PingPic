export { dailyBatchJob } from "./scheduled/dailyBatchJob";

// TODO: as HTTP-callable functions are added (e.g. an admin-panel-triggered
// "adopt suggestion into slot" action, or a group-join callable), export
// them from here too.
